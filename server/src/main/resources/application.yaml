spring:
  application:
    name: lsn-server
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://admin:admin@192.168.1.3:27018/lsn?authSource=admin}
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8383
